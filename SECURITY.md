# 安全策略

## 支持范围

项目尚未发布稳定版。当前只对最新主分支和最新 GitHub Release 进行安全修复；预览版本可能包含尚未完成的安全边界，请勿用于保存不可恢复的重要数据。

稳定版发布后，本页将补充明确的版本支持矩阵。

## 私密报告漏洞

请不要为未修复漏洞创建公开 Issue，也不要在 Gitee 镜像公开披露。

优先使用 GitHub 仓库的 **Security > Advisories > Report a vulnerability** 私密报告功能：

<https://github.com/openartifact-labs/local-agent-runtime-console/security/advisories/new>

报告建议包含：

- 受影响版本、操作系统和安装方式。
- 可复现步骤与预期影响。
- 最小化的概念验证。
- 已完成脱敏的日志或截图。
- 是否已经向其他人公开。

不要发送真实账号密码、Token、Cookie、完整用户数据库、私有源码或未经授权的第三方数据。维护者会尽力在 7 天内确认收到，并在完成影响评估后协商披露时间。

## 重点安全边界

以下问题应作为安全漏洞私密报告：

- 未经用户操作将提示词、输出、路径或运行数据发送到外部网络。
- 读取、记录或泄露 Codex 及其他第三方登录凭据。
- Local API 被非本机来源访问，或绕过来源限制发起任务、执行命令、读取输出。
- 路径穿越、任意文件读写、任意命令执行或桌面导航逃逸。
- 更新包、迁移或插件机制导致代码执行或持久化数据破坏。
- SQLite 数据库迁移可稳定造成静默丢失或越权读取。

一般功能错误、界面问题和非敏感崩溃请使用公开 Issue 模板。

## 安全默认值

- Local API 只监听 `127.0.0.1`。
- 不保存 Codex 账号密码、Token 或 Cookie。
- 不启用默认遥测和后台上传。
- 用户数据默认只保存在本机 SQLite。
- 外部链接、文件和命令能力遵循最小权限原则。

任何削弱这些默认值的改动都需要安全评审、文档更新和回归测试。
