# Local Agent Runtime Console 协作规则

本文件约束在本仓库中工作的开发者与代码 Agent。

## 沟通与文本

- 面向用户的界面、文档、注释和日志默认使用中文；代码标识、协议字段和专有名词保留原文。
- 修改中文文本后检查 UTF-8 编码与乱码，不提交包含不可识别字符的文件。
- 注释重点说明设计意图、隐私边界和复杂分支原因，不逐行复述代码。

## 产品边界

- 本项目是单用户、本地优先的 AI Agent 运行观测台，不是远程控制平台或账号托管服务。
- 当前只实现 `CodexProvider`，但通用领域对象不得依赖 Codex 专有字段。
- Codex CLI 由用户自行安装和登录。禁止读取、记录、上传账号密码、Token、Cookie 或其他登录凭据。
- 用户数据默认只写入本机 SQLite。未经独立设计评审，不得增加远程数据库、遥测、云同步或后台上传。
- GitHub 是主仓库和 Release 来源；Gitee 仅做同步镜像。

## 隐私与安全

- 不得把真实提示词、运行输出、工作目录、用户名、设备路径、环境变量或本机 Codex 会话写入测试夹具、文档和日志。
- 示例使用虚构、最小化数据；路径使用 `<workspace>`、`/path/to/project` 等占位符。
- Local API 默认只监听 `127.0.0.1`。修改监听范围、文件访问、命令执行或任务发起权限时，必须同步评估威胁模型。
- 日志默认不得输出完整提示词、命令参数和 Agent 输出；调试日志也必须可显式关闭。

## 工程约束

- Provider 专有协议只存在于对应适配层；Web 与通用服务只依赖 `packages/contracts` 中的契约。
- SQLite 迁移使用可审计、可重复执行的版本化迁移，禁止在启动阶段静默破坏用户数据。
- 已发布迁移不得原地改写；需要修复时新增迁移并提供升级与回滚说明。
- 保持改动范围最小，不顺带重构无关模块，不覆盖用户尚未提交的修改。
- 新增依赖前评估许可证、体积、跨平台支持和维护状态。

## 验证要求

根据改动范围执行以下命令，并在 Pull Request 中记录结果：

```bash
pnpm typecheck
pnpm test
pnpm build
git diff --check
```

涉及桌面封装时至少验证 Windows，并说明 macOS、Linux 是否由 CI 验证。涉及数据库时增加迁移测试和旧数据升级测试；涉及 Provider 时覆盖断线、重连、超时和不完整事件。

## 文档治理

- `README.md` 保持项目定位、边界、开发入口和文档索引。
- 长期设计记录放在 `docs/`；领域术语维护在 `CONTEXT.md`；重要取舍维护在 `docs/专题/架构决策.md`。
- 代码、配置、数据边界或发布流程变化时，同步更新对应文档。
- 不使用日期型零散文档代替长期文档；历史变化由 Git 与 Release Notes 记录。

## Git 约束

- 未经用户明确要求，不执行提交、推送、强制更新、变基或历史改写。
- 提交前扫描个人信息、私有地址、凭据和历史品牌名称。
- 不提交 `.env`、SQLite 用户数据、运行日志、构建产物和本机缓存。
