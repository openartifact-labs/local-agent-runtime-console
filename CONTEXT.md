# Local Agent Runtime Console 领域上下文

本文档定义项目的统一业务语言和边界。Codex 是当前接入的运行提供方，不是领域模型本身。

## 项目上下文

Local Agent Runtime Console 是运行在用户电脑上的单用户观测工具。它连接用户已经安装并登录的 AI Agent Runtime，发现任务、发起受管运行并展示本机可观测数据。应用不承担账号体系、远程托管、团队协作或官方计费职责。

## 统一语言

### 运行提供方（Provider）

向观测台提供任务发现、任务发起、运行控制和事件能力的 AI Agent 运行环境。当前唯一实现是 `CodexProvider`。

避免使用：模型、客户端、账号。

### 任务（Runtime Task）

用户与 Provider 之间可持续追加运行的工作单元，对应 Codex 中的 Thread。任务包含稳定标识、标题、目录、最近活动和当前可见状态。

避免使用：单次执行、进程。

### 运行（Runtime Run）

任务中由一次用户输入触发、从开始到结束的一次执行，对应 Codex 中的 Turn。一个任务可以包含多次运行。

避免使用：任务、会话。

### 全局任务（Discovered Task）

由 Provider 发现、但不一定由观测台发起的任务。只保证展示 Provider 和本机持久化记录能够证明的轻量状态。

避免使用：受管任务、完整运行。

### 受管任务（Managed Task）

由观测台发起或明确接管的任务。观测台能够关联其运行标识，并持续记录事件、输出快照和指标。

避免使用：所有已发现任务。

### 子任务（Subtask）

由某个父任务中的 Agent 分派或创建、且存在可验证父子关系的任务。父子关系必须来自 Provider 事件或稳定标识，不通过标题或时间相近推断。

### 运行事件（Runtime Event）

运行过程中按时间发生的结构化事实，例如输入接收、推理、工具调用、输出更新、等待用户或运行完成。

避免使用：未经结构化的日志行。

### 输出快照（Output Snapshot）

某一时刻可供界面展示的累计 Agent 输出。快照可能是中间状态，不等同于最终答案。

### 可观测状态（Observation State）

观测台根据最近事件能够确认的状态。缺少证据时使用未知、失联或不可见，不把客户端是否打开等 UI 现象直接解释为任务状态。

### 本机使用分析（Local Usage Analytics）

基于本机可访问 rollout 或运行记录计算的 Token、模型、来源和 Skill 汇总。它不是账号级账单，也不保证覆盖其他设备或已清理的数据。

## 数据边界

- 用户数据默认只保存在本机 SQLite 数据库和 Provider 自身的本机目录中。
- 观测台不保存 Codex 登录凭据，也不代替 Codex CLI 完成登录。
- GitHub 和 Gitee 只承载源码、Issue 与发布产物，不接收运行数据。
- 删除应用不等于删除 Codex 自身数据；清理功能必须区分观测台数据库与 Provider 原始记录。

## 不变量

1. 任何状态必须能追溯到 Provider 事实或本机持久化事件。
2. 通用领域模型不暴露 Provider 专有协议字段。
3. 未经用户明确操作，不将本机运行内容发送到外部服务。
4. 迁移失败不得静默丢弃或覆盖用户数据库。
5. 全局任务与受管任务的观测能力必须在界面和接口中明确区分。
